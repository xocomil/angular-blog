<script lang="ts">
  import Header from './(home)/header.component.analog' with {analog: 'imports'};
  import ArticleContainer from './(home)/article-container.component.analog' with {analog: 'imports'};

  defineMetadata({
    host: {
      class: 'flex flex-col h-full w-full max-h-full overflow-hidden'
    }
  })
</script>

<template>
  <Header class="flex-none"></Header>
  <ArticleContainer></ArticleContainer>
</template>
